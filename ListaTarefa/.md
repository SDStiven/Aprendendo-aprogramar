# ListaTarefa

## Descrição

## Instruções de Uso
- npx tsc --watch      # Compila o arquivo app.ts e fica observando mudanças automaticamente

- npx tsc app.ts --ignoreConfig  # Compila o arquivo app.ts sem usar o arquivo tsconfig.json

- npx tsc app.ts --watch --ignoreConfig  # Compila o arquivo app.ts e fica observando mudanças automaticamente sem usar o arquivo tsconfig.json